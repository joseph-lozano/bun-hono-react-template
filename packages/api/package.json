{
  "name": "api",
  "devDependencies": {
    "@types/bun": "^1.0.0"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --minify --target bun --outdir ../../build/api",
    "start": "NODE_ENV=production bun run src/index.ts"
  },
  "type": "module"
}
